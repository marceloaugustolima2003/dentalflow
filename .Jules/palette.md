# Palette's Journal 🎨

## Critical Learnings
- **Accessibility**: Icon-only buttons must have an `aria-label` attribute describing the action.
- **Accessibility**: SVGs used as icons inside buttons should have `aria-hidden="true"` to avoid redundant announcements.
